import claude from './claude.jsx'
import codex from './codex.jsx'

// Provider registry (frontend). Add a provider = add its config here.
export const PROVIDERS = { claude, codex }
export const PROVIDER_LIST = [claude, codex]
